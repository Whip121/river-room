{
  "name": "river-room",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "start": "node server.mjs", "build": "node build-standalone.mjs", "test": "node --test test/*.test.mjs" },
  "engines": { "node": ">=20" }
}
